//if and comparation operators

const id = 100;

// = assign
// == compare != not
// === exact value and type  !== not

// LOGICAL OPERATORS

// < less than  5 < 7 variation <= less or equal
// > greater than 5 > 3 variation >= greater or equal
// && and - both condition must be true -ex: if(a = b && b = c){ do something }else{ do something else}
// || or - one of the conditions must be true -ex: if(a = b || b = c){ do something }else{ do something else}

// test the typeof a variable

// if(typeof id !== 'undefined'){
//   console.log(`the id is ${id}`);
// }else{
//   console.log(' no id')
// }
// TERNARY OPERATORS ?

console.log(id === 100 ? 'CORRECT' : 'INCORECT');